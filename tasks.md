- [x] Research existing `logs` command implementation and log streaming logic in `src/cli/index.ts` and `src/cli/client.ts`.
- [x] Create a log formatting helper module (e.g., `src/cli/log-formatter.ts`) for processing Winston JSON lines.
- [x] Implement `parseLogLine` function to handle JSON parsing with fallback to raw string.
- [x] Implement timestamp formatting to `[YYYY-MM-DD HH:mm:ss]` with Cyan color support.
- [x] Implement color-coded log level formatting: `[INFO]` (Green), `[WARN]`/`[WARNING]` (Yellow), `[ERROR]` (Red).
- [x] Implement metadata filtering to exclude redundant keys (`timestamp`, `level`, `message`, `workflow`).
- [x] Implement metadata rendering: inline for small objects, indented multiline for large objects/stacks.
- [x] Implement ANSI color detection based on `process.stdout.isTTY`.
- [x] Update the `logs` command definition in `src/cli/index.ts` to accept `--json` and `--raw` flags.
- [x] Integrate the log formatter into the `afk logs` command stream processing.
- [x] Create unit tests for the log formatter covering various log formats and metadata scenarios.
- [x] Create integration tests for `afk logs` command to verify flags and TTY behavior.
